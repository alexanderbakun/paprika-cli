- Init: init should link paprika-cli to local so that new tasks can use .core
- Init: certain commands can have in their config, init_required: (true/false)
- add the inquirer features: default, filter, validate
- get rid of commander and make our own helper so that dynamic configs can be supported on helper

